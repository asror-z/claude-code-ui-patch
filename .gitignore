node_modules/
out/
*.vsix
/build/
.vscode-test/
.DS_Store

# A parent-directory global .gitignore has a blanket *.svg rule (for unrelated
# scratch/export folders); this extension's Activity Bar icon is a real tracked
# source asset, not a generated/scratch file, so it needs an explicit exception.
!media/*.svg
/.claude/Chat Patch Extend/Tests/
